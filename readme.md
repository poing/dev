This is a metapackage to aid in package development.
It points a directory named `composer` in the users home directory.
Allowing packages to be loaded from `~/composer/<vendor>/<name>`.

```
composer require poing/dev
```
